filename = 'saida.txt';
m = csvread(filename);

figure;
plot(m(:,1), m(:, 2), 'r', m(:,1), m(:, 3), 'b');
title("Lista versus Árvore de busca binária");

figure;
plot(m(:,1), m(:, 3), 'b');
title("Árvore de busca binária");

figure;
plot(m(:, 1), m(:, 2), 'r');
title("Lista");
xlabel('Tamanho (n)')
ylabel
